const BASE = '/home/dimon/simplify-showcase'
// const BASE = '.'

const POS_ROOT = `${BASE}/openpose`
const POS_DATA = `${POS_ROOT}/input`
const POS_OUTPUT = `${POS_ROOT}/output`
const POS_BACKLOG = `${POS_ROOT}/backlog`

const SM_ROOT = `${BASE}/sm`
const SM_DATA = `${SM_ROOT}/input`
const SM_OUTPUT = `${SM_ROOT}/output`

module.exports = {
    BASE,
    POS_ROOT,
    POS_DATA,
    POS_BACKLOG,
    POS_OUTPUT,
    SM_ROOT,
    SM_DATA,
    SM_OUTPUT
}